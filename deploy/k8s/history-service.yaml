apiVersion: apps/v1
kind: Deployment
metadata:
  name: history-service
  namespace: ai-infra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: history-service
  template:
    metadata:
      labels:
        app: history-service
    spec:
      containers:
      - name: history-service
        image: ghcr.io/example/history-service:latest
        env:
        - name: STORE_BACKEND
          value: "mysql"
        - name: MYSQL_DSN
          valueFrom:
            secretKeyRef:
              name: ai-infra-data-backends
              key: mysql_dsn
        - name: REDIS_ADDR
          valueFrom:
            secretKeyRef:
              name: ai-infra-data-backends
              key: redis_addr
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ai-infra-data-backends
              key: redis_password
        - name: REDIS_DB
          value: "0"
        - name: CACHE_TTL
          value: "60s"
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: history-service
  namespace: ai-infra
spec:
  selector:
    app: history-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
