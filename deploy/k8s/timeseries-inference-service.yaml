apiVersion: apps/v1
kind: Deployment
metadata:
  name: timeseries-inference-service
  namespace: ai-infra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: timeseries-inference-service
  template:
    metadata:
      labels:
        app: timeseries-inference-service
    spec:
      containers:
      - name: timeseries-inference-service
        image: ghcr.io/example/timeseries-inference-service:latest
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: timeseries-inference-service
  namespace: ai-infra
spec:
  selector:
    app: timeseries-inference-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
